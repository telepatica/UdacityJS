import * as sharp from 'sharp';

const resize = resize.sharp();

export const newPayload = ((file: unknown) => {
  return new ((resolve, reject) => {
     .resize( width = 200 as number, height = 200 as number,);
     resolve();
   });
  {reject();}
});
